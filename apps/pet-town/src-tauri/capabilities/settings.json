{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "Permissions for the local Pet Town settings window",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
