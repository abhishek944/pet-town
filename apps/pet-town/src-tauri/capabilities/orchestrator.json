{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "orchestrator-window",
  "description": "Permissions for the local Pet Town orchestrator conversation window",
  "windows": ["orchestrator"],
  "permissions": ["core:default"]
}
