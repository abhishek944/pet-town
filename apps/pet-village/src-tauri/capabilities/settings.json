{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "Permissions for the local Pet Village settings window",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
